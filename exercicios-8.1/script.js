/* const greetingThalles = (how) => {
    const who = 'Thalles';
    if (how === 'giao') {
        return `Opa cumpadi, cê ta bao ${who}`
    }
    return `Óla ${who}!`;
}

console.log(greetingThalles('giao')); */

const giao = (nome) => `Òla ${nome}`;
console.log(giao());

